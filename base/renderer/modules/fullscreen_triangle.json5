{
  name: "fullscreen_triangle",
  source: "
    f32x4 fullscreen_triangle() {
      f32x2 xy = -1.0 + f32x2((rx_vertex_id & 1) << 2, (rx_vertex_id & 2) << 1);
      return f32x4(xy, (xy + 1.0) * 0.5);
    }
  "
}
